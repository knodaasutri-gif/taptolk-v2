# Tap Talk コードレビュー・評価レポート

レビュー対象: `index.html`、`script.js`、`style.css`、`README.md`

## 総評

Tap Talk は、定型文カード、自由入力、音声入力、読み上げ、お気に入り、辞書、休暇連絡テンプレートを備えた、目的が明確で実用性の高いプロトタイプです。触覚フィードバック、効果音、タイムライン表示など、操作結果を分かりやすくする工夫も見られます。

一方で、機能追加に伴う重複実装、保存データの検証不足、ユーザー入力を HTML として扱う箇所、音声認識の例外時フローなどに改善余地があります。公開・継続開発の前に基盤を整理することを推奨します。

**総合評価: C+（機能は充実しているが、安定性・保守性・アクセシビリティを改善する段階）**

## 1. 構造と品質

### 良い点

- 機能ごとに関数が分かれており、カード描画、音声入力、辞書、休暇文生成の役割は把握しやすい構成です。
- 音声認識では `isListening`、`isStarting`、再起動タイマーを用い、`recognition.start()` の多重実行を避けようとしています。
- お気に入り読み込みは `try/catch` と配列判定を行っており、保存データ破損への最低限の配慮があります。
- 画面への通常テキスト表示には `textContent` を使う箇所があり、チャット表示は安全に実装されています。

### 課題

1. **HTML の重複 ID と未定義関数**
   - `categoryTabs` が `index.html` 内で二重に定義されています。
   - 後方の静的タブにある `filterCategory('all')` は JavaScript 側で定義されていません。クリック時にエラーとなり、カテゴリUIも重複表示されます。

2. **関数の重複定義**
   - `setQuickDate()` と `clearDisplay()` がそれぞれ二度定義されています。JavaScript では後方の定義が前方を上書きするため、修正時の不整合や意図しない挙動につながります。
   - `speakText()` では効果音が二回呼ばれており、意図しない二重再生の可能性があります。

3. **安全でない DOM 文字列生成**
   - カードの `icon`、`text`、`id` をテンプレート文字列で `innerHTML` に埋め込んでいます。カードはユーザー入力から作成できるため、HTML/JavaScript 注入のリスクがあります。
   - 辞書一覧の削除ボタンも、ユーザー入力のキーをインライン `onclick` に埋め込んでいます。
   - `createElement()`、`textContent`、`dataset`、`addEventListener()` に統一し、ユーザー入力を HTML として解釈させない実装に変更すべきです。

4. **保存データの例外処理・検証不足**
   - カード保存、辞書保存、辞書の `JSON.parse()` は一貫して保護されていません。
   - 壊れた JSON、想定外のデータ構造、ストレージ容量超過、プライベートモード等で、一部または全体の機能が停止する可能性があります。
   - 保存データにバージョンを付け、読み込み時に配列・必須プロパティを検証し、異常時はバックアップ後に初期化する設計が望まれます。

5. **古い表示ロジックの残存**
   - `speechPlaceholder` と `speechText` を操作するコードがありますが、対応する要素は HTML に存在しません。
   - 現行のタイムライン表示へ統一し、未使用コードを削除すると保守性が上がります。

## 2. UX・操作性

### 良い点

- カード選択だけで読み上げられるため、頻出フレーズの利用が速いです。
- 自由入力、辞書置換、休暇連絡テンプレートは実用場面に合っています。
- 音声認識中のボタン表示、Toast、バイブレーション、効果音により、操作状態が伝わりやすくなっています。
- カテゴリタブの横スクロールと2列カードは、スマホ画面を意識した基本設計です。

### 改善提案

1. **音声認識の自動再起動をエラー種別ごとに制御する**
   - 現在は `onend` では再起動しますが、`onerror` が発生すると listening 状態を解除します。
   - `no-speech` や一時的な `network` は、回数上限付きで再試行する方が自然です。
   - `not-allowed` や `service-not-allowed` は再試行せず、権限設定または自由入力へ誘導すべきです。

2. **手動停止とエラー停止を区別する**
   - 利用者が停止ボタンを押した場合に `aborted` エラーの警告が出る可能性があります。
   - `isUserStopping` のようなフラグを設け、意図した停止にはエラー通知を出さないようにします。

3. **連続認識の結果を堅牢に扱う**
   - 現状は最後の認識結果だけを処理しています。
   - `event.resultIndex` 以降を順に処理し、確定結果だけをタイムラインへ追加すると、取りこぼしや重複を抑えられます。

4. **小型画面での操作領域を見直す**
   - 操作ボタンを常時5列にしているため、小型スマホでは文字が詰まり、押下領域も小さくなりやすいです。
   - 主操作を「音声入力」「自由入力」に絞り、作成・編集・辞書をメニューへ集約する案が有効です。

5. **モーダルとキーボード操作を整備する**
   - モーダルの Escape 閉じ、背景クリック、フォーカストラップ、開閉時のフォーカス移動を追加します。
   - カードは `div` ではなく `button` とし、Enter/Space でも実行できるようにします。

6. **視覚的なアクセシビリティを改善する**
   - 入力欄に `outline: none` が指定され、十分な代替フォーカス表示がありません。
   - `:focus-visible` で明確な輪郭を設け、色だけに依存しない状態表示を追加します。

## 3. ブラウザ・スマホ互換性

### 現状の評価

- `SpeechRecognition` と `webkitSpeechRecognition` の両方を確認しており、Chromium 系と一部 Safari を意識した実装です。
- 非対応時にマイクボタンを隠すため、音声非対応環境でアプリ全体が止まることは避けられています。
- `speechSynthesis`、Web Audio、バイブレーションはいずれも端末・ブラウザごとの差があるため、機能の有無と失敗時を前提に扱う必要があります。

### 考慮漏れ・リスク

1. **音声認識の実装差**
   - Web Speech API の音声認識はブラウザ間の差が大きく、Firefox では前提にできません。
   - Chromium 系でも認識がネットワーク上のサービスに依存する場合があり、オフラインやサービス障害時に失敗します。

2. **非対応時の案内不足**
   - マイクボタンを隠すだけでは、利用者は音声入力が使えない理由を理解できません。
   - 「このブラウザでは音声入力を利用できません。自由入力をお使いください」という常設メッセージと、自由入力への導線を表示します。

3. **HTTPS とマイク許可**
   - 実運用でのマイク利用には HTTPS と利用者の許可が必要です。
   - 初回利用時に権限が必要な理由を説明し、拒否時には再設定方法を案内します。

4. **モバイル固有の確認不足**
   - iPhone Safari と Android Chrome で、認識中の画面ロック、バックグラウンド復帰、Bluetoothイヤホン、通信切替、マイク権限拒否後の再許可を実機テストする必要があります。
   - 音声合成の声・速度・日本語品質も端末で差が出るため、読み上げ速度や音量を設定可能にすると実用性が高まります。

## 4. 今後の改善提案

### 最優先: 安定化・安全化

1. 重複した `categoryTabs`、静的タブ、重複関数、未使用の表示ロジックを削除する。
2. インライン `onclick` と `innerHTML` によるユーザー入力の描画を廃止し、DOM API とイベントリスナーに統一する。
3. 保存データを単一のバージョン付きスキーマに統合し、例外処理、検証、復旧を実装する。
4. 音声認識を `idle`、`starting`、`listening`、`stopping`、`error` の状態モデルで一元管理する。

### 次段階: UX のブラッシュアップ

- 最近使ったカード、検索、カードの長押し編集、ドラッグによる並べ替えを追加する。
- 休暇・連絡・病院・買い物など、用途別テンプレートセットを用意する。
- 読み上げ速度、音量、音声、効果音、振動の設定画面を追加する。
- 認識中テキストと確定済みの会話履歴を分け、誤認識の修正や再発話を可能にする。
- カード、辞書、お気に入りの JSON バックアップ・復元と共有機能を追加する。

### 中長期: 配布・継続利用

- PWA 化し、ホーム画面追加、アプリアイコン、オフライン時のカード閲覧・読み上げを提供する。
- 実機テスト表を整備し、iOS Safari、Android Chrome、PC Chrome/Edge/Safari、Firefox の対応範囲を明示する。
- 重要な操作と音声認識フローに対する手動テスト手順、可能であればユニットテストを追加する。

## 推奨する改善順

1. 重複・未定義処理の除去
2. DOM 生成の安全化
3. 保存データの検証と例外処理
4. 音声認識状態管理と再試行方針の整理
5. モバイルUI・アクセシビリティ改善
6. PWA、バックアップ、検索・並べ替え等の拡張

